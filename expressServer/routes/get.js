// would create an app.use route that would map to this file 
// Data access Layer

// express server (apis) CRUD express application which accesses your Mongo DB  Then the Mongo DB has THE connection to the DB with Music files. 
// You want DAL between the client and the DB. 